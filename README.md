### 클래스 정의

- Main : 시작 지점이 되는 클래스, `public static void main(String[] args)`
- Product: 개별 상품 정보를 가지는 클래스
    - 상품명, 가격, 설명, 재고수량
        - 예시: Galaxy S24, 1200000, 최신 스마트폰, 50
        - `new Product("Galaxy S24", 1200000, "최신 스마트폰", 50)`
- CommerceSystem: 프로그램 비즈니스 로직 클래스
- Category: Product 클래스를 관리하는 클래스
    - 예시 : 전자제품, 의류, 식품 등 각 카테고리 내에 여러 `Product`를 포함합니다.

### STEP 1. 객체 지향 설계를 적용해 상품 관리 시스템을 프로그래밍해보자

- [x]  **상품 목록 출력**
    - [x]  JAVA 프로그램을 실행하면 여러 전자제품을 출력합니다.
    - [x]  제시된 상품 중 입력받은 숫자에 따라 다른 로직을 실행하는 코드를 작성합니다.
- [x]  **`Product` 클래스 생성하기**
    - [x]  설명 : 개별 상품을 관리하는 클래스입니다. 현재는 전자제품만 관리합니다.
    - [x]  클래스는 `상품명`, `가격`, `설명`, `재고수량` 필드를 갖습니다.
- [x]  `main` **함수에서 `Product` 클래스를 생성하여 상품 목록을 추가합니다.**
    - [x]  `Product` 객체 생성을 통해 `상품명`, `가격`, `설명`, `재고수량`을 세팅합니다.
        - [x]  키워드: `new`
    - [x]  `List`를 선언하여 여러 `Product`을 추가합니다.
        - [x]  `List<Product> products = new ArrayList<>();`
    - [x]  반복문을 활용해 `products`를 탐색하면서 하나씩 접근합니다.
- [x]  **STEP 1을 구현하면 터미널에 이렇게 보여집니다.**

   ```java
   [ 실시간 커머스 플랫폼 - 전자제품 ]
   1. Galaxy S25     | 1,200,000원 | 최신 안드로이드 스마트폰
   2. iPhone 16      | 1,350,000원 | Apple의 최신 스마트폰
   3. MacBook Pro    | 2,400,000원 | M3 칩셋이 탑재된 노트북
   4. AirPods Pro    |   350,000원 | 노이즈 캔슬링 무선 이어폰
   0. 종료           | 프로그램 종료
   0 <- // 0을 입력
   
   커머스 플랫폼을 종료합니다.
   ```
---
### **STEP 2**. 객체 지향 설계를 적용해 순서 제어를 클래스로 관리하기

- [x]  **`CommerceSystem` 클래스 생성하기**
    - [x]  **설명**: 커머스 플랫폼의 상품을 관리하고 사용자 입력을 처리하는 클래스입니다.
    - [x]  `Product`를 관리하는 리스트가 필드로 존재합니다.
    - [x]  `main` 함수에서 관리하던 입력과 반복문 로직은 이제 `start` 함수를 만들어 관리합니다.
    - [x]  `List<Product> products` 는 `CommerceSystem` 클래스 생성자를 통해 값을 할당합니다.
        - [x]  `CommerceSystem` 객체를 생성하고 사용하는 `main` 함수에서 객체를 생성할 때 값을 넘겨줍니다.

---
### **STEP 3**. 객체 지향 설계를 적용해 상품 카테고리와 고객 관리를 클래스 기반으로 관리하기

- [x]  **`Category` 클래스 생성하기**
    - [x]  설명 : Product 클래스를 관리하는 클래스입니다.
    - [x]  전자제품, 의류, 식품 등 각 카테고리 내에 여러 `Product`를 만들어 줍니다.
    - [x]  `List<Product>` 은 `CommerceSystem` 클래스가 관리하기에 적절하지 않으므로 Category 클래스가 관리하도록 변경합니다.
    - [x]  여러 상품들을 포함하는 상위 개념 '전자제품' 같은 `카테고리 이름` 필드를 갖습니다.
    - [x]  카테고리 이름을 반환하는 메서드가 구현되어야 합니다.
- [x]  **`Customer` 클래스 생성하기**
    - [x]  설명 : 고객 정보를 관리하는 클래스입니다.
    - [x]  클래스는 `고객명`, `이메일`, `등급` 필드를 갖습니다.
---
### **STEP 4**. 캡슐화 적용하기

- [x]  `Product`, `Category`, `Customer` 그리고 `CommerceSystem` 클래스의 필드에 직접 접근하지 못하도록 설정합니다.
- [x]  Getter와 Setter 메서드를 사용해 데이터를 관리합니다.
---
### Lv 1. 장바구니 및 주문하기 기능을 추가하기

- [x]  **장바구니 생성 및 관리 기능**
    - [x]  사용자가 선택한 상품을 장바구니에 추가할 수 있는 기능을 제공합니다.
    - [x]  장바구니는 상품명, 수량, 가격 정보를 저장하며, 항목을 동적으로 추가 및 조회할 수 있어야 합니다.
    - [x]  사용자가 잘못된 선택을 했을 경우 예외를 처리합니다(예: 유효하지 않은 상품 번호 입력)
- [x]  **재고 관리 시스템**
    - [x]  상품을 장바구니에 담을 때 재고를 확인하고, 재고가 부족할 경우 경고 메시지를 출력합니다.
    - [x]  주문 완료 시 해당 상품의 재고를 차감합니다.
- [x]  **장바구니 출력 및 금액 계산**
    - [x]  사용자가 주문을 시도하기 전에, 장바구니에 담긴 모든 상품과 총 금액을 출력합니다.
    - [x]  출력 예시
        - [ ]  각 상품의 이름, 가격, 수량
        - [ ]  총 금액 합계
- [x]  **장바구니 담기 기능**
    - [x]  상품을 선택하면 장바구니에 추가할 지 물어보고, 입력값에 따라 "추가", "취소" 처리합니다.
    - [x]  장바구니에 담은 목록을 출력합니다.
- [x]  **주문 기능**
    - [x]  장바구니에 담긴 모든 항목을 출력합니다.
    - [x]  합산하여 총 금액을 계산하고, "주문하기"를 누르면 장바구니를 초기화하고 재고를 차감합니다.
---
### LV 2. 관리자 모드 추가

- [x]  **관리자 인증 기능**
    - [x]  관리자용 비밀번호 설정 및 인증 시스템 구현
    - [x]  메인 메뉴에 "6. 관리자 모드" 옵션 추가
    - [x]  비밀번호 입력 시 3회 실패하면 메인 메뉴로 돌아가기
- [x]  **상품 추가 기능**
    - [x]  새로운 상품의 정보를 입력받아 기존 카테고리에 추가
    - [x]  입력받을 정보: 상품명, 가격, 설명, 재고수량
    - [x]  카테고리 선택 후 해당 카테고리에 상품 추가
    - [x]  중복 상품명 검증 (같은 카테고리 내에서)
- [x]  **상품 수정 기능**
    - [x]  기존 상품의 정보 수정 (가격, 설명, 재고수량)
    - [x]  상품명으로 검색하여 수정할 상품 선택
    - [x]  수정할 항목 선택 후 새로운 값 입력
- [x]  **상품 삭제 기능**
    - [x]  기존 상품을 카테고리에서 제거
    - [x]  삭제 전 확인 메시지 출력
    - [x]  삭제된 상품이 장바구니에 있다면 장바구니에서도 제거

